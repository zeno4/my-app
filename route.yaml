apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: engin1-route
  namespace: test-argocd
spec:
  host: enginx1.apps-crc.testing 
  port:
    targetPort: 8080 
  to:
    kind: Service
    name: nginx-service
